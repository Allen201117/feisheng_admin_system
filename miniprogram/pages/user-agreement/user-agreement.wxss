/* Policy section styles */
.policy-section-title { display:block; margin-top:var(--sp-2); font-size:var(--fs-base); font-weight:var(--fw-semibold); color:var(--text-primary); }
.policy-section-text { display:block; margin-top:8rpx; font-size:var(--fs-sm); color:var(--text-secondary); line-height:1.8; }
