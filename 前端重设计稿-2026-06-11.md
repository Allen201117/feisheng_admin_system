# 飞盛小程序前端重设计稿

> 版本：2026-06-11  
> 状态：设计稿，未进入代码实现  
> 适用范围：微信小程序原生 WXML / WXSS / JS，26 个页面，老板端、员工端、质检端、平台端

## 1. 设计结论

新版方向不继续扩大“Apple grouped”的皮肤感，而是收敛为 **工业运营工作台**。

它保留现有高对比、大字号、浅灰页面底和白色分组，但减少泛白卡片和平均化入口，把页面改成更强的任务流：老板先看到经营风险与生产进度，员工先完成打卡和报工，质检员先处理待检，平台管理员先选定工厂再维护订阅和账号。

一句话标准：**打开即知道要处理什么，点下去之前知道后果。**

## 2. 当前现状判断

从代码和文档看，飞盛不是一个展示型产品，而是计件工资、订单、考勤、质检、订阅和多工厂隔离共同构成的生产运营工具。核心闭环是：

```text
订单 -> 工序分配 -> 员工报工 -> 质检 -> 工资核算 -> 发薪锁定 -> 统计 -> 导出
```

现有前端已经有一套全局 token 和共享模板，问题不在“完全没有设计”，而在几个系统性断点：

- 页面入口平均化：老板首页大量同等卡片，不能优先暴露异常、待办和高频动作。
- 信息层级过钝：`--text-secondary`、`--text-tertiary` 被设成主黑，虽然可读但层级损失明显。
- 卡片使用过多：许多页面变成白卡片堆叠，业务关系没有被表达出来。
- 内联样式过多：WXML 内至少 273 处 `style=""`，尤其数据中心表格、详情页弹窗和局部筛选。
- 高风险动作位置不够稳定：清空工价、清空报工、删除、停用、发薪等动作需要独立危险区和二次确认。
- 表格没有系统化：数据中心和导出预览仍像临时拼出来的横向滚动表。

## 3. 三个设计方向

### 方向 A：高对比 Apple grouped 继续精修

保留当前视觉方向，只修颜色、间距、按钮、空状态和部分页面卡片。风险低，改动快，但很难解决老板端入口平均、订单详情复杂、数据表格粗糙的问题。

适合只做短期提质，不适合这次“重新设计前端”。

### 方向 B：工业运营工作台（推荐）

以手机上的生产看板为核心隐喻：顶部是状态，正文是任务流，底部或危险区承载操作。视觉上保持克制，不引入复杂动画和新框架。通过组件规则把订单卡、工序行、报工行、数据表格、状态标签和确认弹窗统一起来。

这是最符合当前产品和代码结构的方向：它能在不推翻小程序原生栈的前提下明显提升可用性。

### 方向 C：轻量数据驾驶舱

老板端强化 KPI 和图表，数据中心变成更完整的 dashboard。视觉上更“高级”，但对员工端、质检端帮助有限，也容易把内部工具做成演示型大屏。

不建议作为主方向，只可在数据中心局部吸收。

## 4. 推荐方向：工业运营工作台

### 视觉关键词

- 可靠：浅灰页面底、白色主面板、清楚边界，避免漂浮和炫技。
- 高对比：关键字段不做低灰，状态文字和单位完整显示。
- 密而有序：老板端允许较高信息密度，但必须用分区、吸顶筛选和表格规则管理。
- 强触控：员工、质检和危险确认按钮要大，避免现场误触。
- 少动效：只有按压、加载骨架、切换状态和底部面板进入需要动效，控制在 150-200ms。

### 色彩策略

保留一主三语义，但修正层级：

| 用途 | 建议 token | 说明 |
|---|---|---|
| 页面底 | `--bg-page #F4F5F7` | 比现有略冷，像工作台底板 |
| 主卡片 | `--bg-card #FFFFFF` | 只用于真正分组 |
| 次表面 | `--bg-panel #F8FAFC` | 表格、筛选、列表区域 |
| 主文字 | `--text-primary #18181B` | 标题、金额、姓名、订单 |
| 次文字 | `--text-secondary #3F4652` | 说明和元信息，不再等同主黑 |
| 弱文字 | `--text-muted #667085` | 只用于非关键说明，必须过对比检查 |
| 主操作 | `--blue-600 #2563EB` | 保存、查看、当前选中 |
| 生产完成 | `--green-600 #16803C` | 完成、已分配、正向收益 |
| 待处理 | `--amber-600 #B45309` | 待检、即将到期、待分配 |
| 风险危险 | `--red-600 #DC2626` | 删除、清空、停用、失败 |

避免新增长条渐变、玻璃拟态、霓虹阴影。小程序现场使用不需要这些。

### 字体与数字

继续使用系统字体，不引入外部字体。中文正文保持 30-32rpx，页面标题 40-48rpx，关键数字 48-60rpx。金额、件数、工时、排名统一启用等宽数字。

标题不使用负字距；小程序工具界面里负字距会让中文密集页面显得挤。

## 5. 信息架构

### 老板端导航重排

老板首页从“入口集合”改为“三段式工作台”：

1. 今日待处理：待质检、待分配、异常考勤、即将到期、未发薪订单。
2. 生产与工资：进行中订单、报工管理、工资发放、数据中心。
3. 管理工具：员工、考勤码、导出、设置、服务续费。

原来的均等菜单保留，但放到第二屏，首屏优先待办和关键指标。

### 员工端导航重排

员工首页首屏固定为：

1. 今日状态：是否在岗、今日工时、今日预估工资。
2. 主要动作：上班/下班打卡，当前可报工工序。
3. 本人结果：我的排名、本月工资、历史报工。
4. 账号工具：扫码打卡、修改密码、退出。

排行榜入口不再和“我的排名”抢位置。首页先给本人排名卡，列表页再看完整榜单或仅本人榜。

### 质检端导航重排

质检员只需要两个层级：

1. 待质检队列：按提交时间、员工、订单、工序、数量展示。
2. 质检详情：合格数输入、异常说明、提交结果。

待检列表应比当前卡片更像队列，减少重复字段，突出“下一条要处理什么”。

### 平台端导航重排

平台端从编号长页面改为“左选工厂，右维护资料”的移动化结构：

1. 工厂列表与搜索。
2. 当前工厂状态摘要。
3. 资料、订阅、管理员三组折叠区。
4. 新增工厂独立入口，避免和编辑当前工厂混在同一个长滚动里。

## 6. 页面级设计稿

### 6.1 登录页

目标：让首次登录和回访登录都清楚，不碰隐私默认同意红线。

结构：

```text
品牌区：logo + 工厂记账系统 + 一句业务范围
登录面板：工厂码 / 姓名 / 手机号 / 密码
辅助区：忘记密码、上次账号提示
底部固定：隐私勾选 + 登录按钮
```

改动要点：

- 保留底部固定隐私勾选，未勾选时登录按钮可点但就地提示“请先勾选协议”，不要弹全局生硬 toast。
- 已记住账号时，把工厂码、姓名、手机号合并成“上次账号”摘要，默认只输入密码，提供“更换账号”展开全部字段。
- 强制改密弹窗改成底部 sheet，标题说明“首次登录需要修改默认密码”，错误写在对应输入框下方。

### 6.2 老板首页

目标：老板打开后先知道今天卡在哪里。

首屏示意：

```text
┌ 飞盛一厂                         退出 ┐
│ 今日 06-11 · 老板工作台              │
│ 本月薪资支出 ¥42,680    进行中订单 12 │
└─────────────────────────────────────┘

待处理
┌ 待质检 18 ┐ ┌ 待分配 6 ┐ ┌ 异常考勤 3 ┐

生产进度
┌ 重点订单：夏季校服  72%  截止还有 4 天 ┐
└ 查看订单 / 报工管理 / 工资发放           ┘

常用管理
员工  考勤  排行榜  数据中心
导出  设置  考勤码  服务续费
```

改动要点：

- “本月薪资支出”仍是老板核心指标，但旁边必须有订单和待检信号，避免只看到钱。
- 入口不再四等分堆叠，按待处理、生产、管理分区。
- 危险或低频设置不进首屏，减少老板误触。

### 6.3 员工首页

目标：员工不需要理解系统结构，只要完成上班、报工、看结果。

首屏示意：

```text
┌ 张三 · 员工工作台          14:32 ┐
│ 今日预估 ¥236.50   今日工时 6.5h │
│ 我的排名 第 8 名    本月 ¥5,842  │
└───────────────────────────────┘

考勤
[ 上班 已开始 08:04 ] [ 下班打卡 ]

今日报工
┌ 缝制 · 夏季校服    ¥1.80/件 ┐
│ 已报 120 / 300 件  40%      │
│ 继续报工                       │
└──────────────────────────────┘
```

改动要点：

- `我的排名` 卡保留醒目，但合入员工结果区，不要把首页切成太多独立白卡。
- 今日报工工序卡只显示和员工动作有关的信息：工序、订单、单价是否隐藏、已报/目标、继续报工。
- 快捷操作收到底部工具区，弱化图标宫格。

### 6.4 订单详情

目标：订单详情要分清“看状态、管工序、做危险动作”。

结构：

1. 订单摘要：状态、总数量、截止日期、时间进度。
2. 工序管理：搜索、筛选、工序列表、添加工序。
3. 分配面板：作为底部全屏 sheet，不挤在详情正文中。
4. 价格与审计：工价隐藏、工价记录、工序汇总。
5. 危险区：清空工价、清空报工、删除订单。

改动要点：

- 危险区固定用红色边框浅红底，和普通卡片分开。
- `工价隐藏` 从摘要卡中移到“价格与审计”，它是配置，不是订单状态。
- 工序卡改成列表行：左侧工序与分配人，中间进度，右侧价格和操作，避免每道工序都像大卡片。
- 已完成订单顶部显示锁定说明：“订单已完成，工序、报工和价格不可修改。”

### 6.5 报工管理

目标：老板要能从订单进度快速定位异常工序，再下钻到报工明细。

结构：

- 顶部 segmented control：进度总览 / 报工明细。
- 进度总览：订单选择、三项统计、工序进度列表。
- 报工明细：维度切换、搜索、分组列表、价格差异提示。

改动要点：

- 进度总览用“工序行”替代一张张卡，支持长订单 150-200 道工序。
- `结算价` 与 `当前价` 只在不一致时显示双价，并用 amber 提醒，不把所有行都变复杂。
- 新增/编辑报工使用底部 sheet，提交错误显示在数量或备注字段下。

### 6.6 质检首页与质检详情

目标：质检就是队列处理，不要做成订单卡浏览。

列表行字段：

```text
员工名     待质检
订单 / 工序
提交 120 件 · 14:28
[进入质检]
```

详情页：

- 顶部显示员工、订单、工序、提交数量。
- 合格数输入区是唯一主控件，支持加减步进。
- 自动显示不合格数量和合格率。
- 提交按钮文案为“提交质检结果”，禁用态写清原因。

### 6.7 数据中心与导出预览

目标：表格要像核算工具，不像临时 HTML 表。

改动要点：

- 抽出 `data-table` 视觉规范：表头深色、数据格固定宽、首列 sticky 的视觉预留、金额右对齐、数量居中、文本左对齐。
- 横向滚动容器顶部显示“左右滑动查看更多字段”提示，只在字段多时出现。
- 预览表上方固定三项摘要：行数、字段数、合计金额或总件数。
- 空状态必须给下一步：调整时间、返回订单列表、重新加载。

### 6.8 平台管理

目标：降低长页面维护成本，减少误改工厂。

结构：

```text
平台管理
[搜索工厂]
工厂列表

当前工厂摘要
资料维护
订阅开通
管理员账号

[新增工厂]
```

改动要点：

- 当前选中工厂用固定摘要条显示，滚动到下方也能知道正在编辑谁。
- 停用工厂和重置管理员密码都进入危险确认，不和保存资料按钮相邻。
- 订阅开通记录用时间线列表，不使用普通卡片堆叠。

## 7. 组件系统

### 页面模板

- `workbench-page`：角色首页，顶部状态 + 待处理 + 入口区。
- `list-page`：搜索筛选 + 列表 + 空/加载/错误。
- `detail-page`：摘要 + 主内容 + 配置 + 危险区。
- `table-page`：筛选 + 摘要 + 横向表格 + 导出动作。
- `form-sheet`：底部表单，用于新增、编辑、改密、补签、报工。

### 核心组件

- 状态标签：`status-pill`，必须有文字，不单靠颜色。
- 统计数字：`metric-block`，固定 label、value、unit、hint。
- 工序行：`process-row`，用于订单详情、报工管理、数据中心。
- 报工行：`worklog-row`，用于员工历史、老板明细、工资详情。
- 危险区：`danger-zone`，浅红底、红边、明确影响范围。
- 空状态：`empty-state`，标题 + 描述 + 一个主动作。
- 错误状态：`inline-error`，出现在字段或操作区旁边。
- 骨架屏：按真实布局占位，不使用居中 spinner。

### 交互规则

- 触控反馈统一 `scale(0.98)` 或背景变深，不做弹跳动画。
- 主要切换用 segmented control，不把 tab、chip、button 混用。
- 底部固定操作要避开 safe area。
- 删除、清空、停用、发薪、改价失败必须说明业务原因。
- 长列表默认分页或分批渲染，不把 100+ 大卡片一次性铺满。

## 8. 文案规则

文案要说清业务后果：

| 场景 | 推荐文案 |
|---|---|
| 已完成订单锁定 | 订单已完成，工序、报工和价格不可修改 |
| 已发薪不可改价 | 该工序已发薪，单价不可修改 |
| 私密排行榜 | 仅显示我的排名，老板开启公开后可查看完整榜单 |
| 表格无数据 | 当前筛选条件下暂无数据，可调整时间后重新加载 |
| 订阅到期 | 服务已到期，新增订单和报工会受限 |

避免“操作失败”“请稍后再试”这种没有业务信息的泛文案。

## 9. 落地优先级

### 第一阶段：设计系统收口

- 修正文字 token 层级，不再把 secondary/tertiary 全部等同主黑。
- 抽 `danger-zone`、`data-table`、`form-sheet`、`process-row`、`worklog-row`。
- 清理共享模板里的内联样式。
- 保留现有业务逻辑，不调整云函数。

### 第二阶段：三张主屏重做

- 老板首页：待处理 + 生产进度 + 管理入口。
- 员工首页：今日状态 + 考勤 + 今日报工 + 我的结果。
- 订单详情：摘要、工序列表、价格审计、危险区分离。

这三张页面决定整体气质，应先做可预览切片。

### 第三阶段：高密度页治理

- 报工管理：工序行和报工行统一。
- 数据中心：表格组件化。
- 平台管理：当前工厂摘要 + 折叠维护区。
- 质检：队列化列表和详情页表单。

## 10. 验收标准

- 微信开发者工具可正常预览核心页面。
- `npm run test:unit` 通过。
- `git diff --check` 通过。
- UI 检查至少覆盖：登录页、员工首页、老板首页、订单详情、报工管理、质检首页、数据中心。
- 关键页面无明显文字溢出、按钮挤压、卡片套卡片、危险动作混入普通操作区。
- 新增或调整的空状态、加载态、错误态都有明确下一步。

## 11. 不做的事

- 不重写技术栈，不引入 React、Tailwind、外部 UI 库或动画库。
- 不改工资、统计、排行榜、发薪、权限和时间口径。
- 不做暗黑主题、营销式首页、复杂图表大屏或装饰性动效。
- 不在一次实现里扫完 26 个页面，先做三张主屏确认方向。
