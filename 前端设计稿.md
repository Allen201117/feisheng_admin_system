# 飞盛小程序前端设计稿

> 当前版本：2026-05-18
> 参考项目：Open Design (`nexu-io/open-design`, 本地下载到 `/private/tmp/open-design`, commit `332e982`)

## 1. 设计结论

飞盛小程序应继续走“高对比 Apple grouped 工厂工作台”方向：白色卡片、浅灰页面底、清晰分组、大字号、强触控区、少装饰。用户主要在工厂现场、手机小屏和光线不稳定的环境下使用，设计优先级不是炫，而是看得清、点得准、找得到。

本轮已把全局灰字体系加深，避免白底上信息发虚。后续所有页面都应优先使用 `app.wxss` 的语义 token，不再随手写浅灰色。

## 2. Open Design 对本项目的帮助

Open Design 本身是 React / Next.js + 本地 daemon 的设计工作流，不适合直接搬到微信小程序里。它的价值在方法层，而不是代码层：

- `DESIGN.md` 结构值得借鉴：把颜色、字体、间距、布局、组件、动效、文案、反模式写成固定规范。
- 方向库值得借鉴：先定视觉方向，再落组件，避免每个页面临时发挥。
- mobile-app skill 值得借鉴：移动端单屏只做一个主要任务，状态栏、触控区、底部安全区和主操作要稳定。
- color / HIG 类 skill 值得借鉴：颜色不是只看好看，要有对比度门槛和可访问性自检。

不建议照搬的部分：

- Open Design 的 HTML artifact、iframe preview、Next.js 组件不能直接用于 WXML/WXSS。
- 它的 Web/桌面 dashboard 布局不适合小程序现场操作。
- 它偏“生成设计产物”的工作流，飞盛更需要“长期一致的业务界面规范”。

## 3. 视觉方向

名称：高对比 Apple grouped 工厂工作台

关键词：

- 白色卡片承载业务信息
- 浅灰页面底做分区
- 蓝色只表示主操作、链接、可点击
- 绿色表示完成、可用、正向收益
- 红色表示危险、失败、删除、不可逆
- 普通业务文字统一主黑，不能再用灰色制造“弱层级”
- 卡片不再只是平白块，使用轻微纵向 surface、细边界和克制阴影形成可扫描层级
- 主操作按钮保留 Apple 蓝，但增加低强度投影，让“可点”更明确
- 分段控件、快捷入口、状态标签统一走 surface token，避免页面之间各自发明浅灰

页面气质：

- 员工端：任务优先，首页先看打卡、报工、工资和常用操作。
- 老板端：扫描效率优先，列表、统计、筛选和批量操作要密但不乱。
- 质检端：判断优先，待检信息和合格/不合格操作要一眼分清。
- 平台端：管理优先，状态、到期、开通和联系人信息要清楚。

## 3.1 Open Design 落地升级

这次不只保留文档，而是把 Open Design 的几个方法落到现有 Apple 风格里：

- 先把 `app.wxss` 当成设计系统入口，新增 `--bg-card-soft`、`--surface-strong`、`--hairline`、`--shadow-card` 等 token。
- 卡片、Hero、快捷入口、订单卡、工序卡、考勤卡统一从纯白平面改为轻微 grouped surface。
- 主按钮、成功按钮、危险按钮统一增加语义阴影，但不做夸张渐变。
- 员工首页和登录页作为高频入口优先精修，让首页功能区和首次登录体验更像一个完整产品。
- 测试层新增 UI 约束，防止后续又退回“平白块 + 浅灰小字”的状态。

## 4. 颜色规范

全局 token 以 `miniprogram/app.wxss` 为唯一真相源：

| 用途 | Token | 当前值 | 使用场景 |
|---|---|---:|---|
| 主文字 | `--text-primary` | `#1D1D1F` | 标题、关键金额、订单名、员工名 |
| 次级文字 | `--text-secondary` | `#1D1D1F` | 说明、标签、列表副信息 |
| 辅助文字 | `--text-tertiary` | `#1D1D1F` | 时间、弱提示、空状态描述 |
| 占位文字 | `--text-placeholder` | `#1D1D1F` | input placeholder、未选择提示 |
| 页面底 | `--bg-page` | `#F5F5F7` | 页面背景 |
| 卡片底 | `--bg-card` | `#FFFFFF` | 信息卡、表单卡、列表卡 |
| 主操作 | `--blue-500` | `#007AFF` | 登录、保存、确认、可点击链接 |
| 成功 | `--green-600` | `#248A3D` | 完成、已分配、收益、可用状态 |
| 警示 | `--amber-600` | `#C87500` | 待处理、提示、价格标签 |
| 危险 | `--red-600` | `#D6241E` | 删除、清空、失败、禁用业务状态 |

对比度规则：

- `--text-secondary` 必须等于主文字色，并在白底上达到 7:1 以上。
- `--text-tertiary` 必须等于主文字色，并在白底上达到 7:1 以上。
- placeholder 必须在白底上达到 7:1 以上。
- 禁止再使用 `#475467`、`#667085`、`#6B7280`、`#6E6E73`、`#8A8F98`、`#98A2B3`、`#A0A4AC`、`#AEAEB3`、`#B0B3BA`、`#C7C7CC` 作为文字颜色。

## 5. 字体与字号

原则：

- 不引入外部字体，继续使用系统字体。
- 小程序面向工厂现场，字号宁可偏大，不做“精致小灰字”。
- 数字、金额、件数、工时应使用等宽数字效果：`font-variant-numeric: tabular-nums`。

字号建议：

| 层级 | Token / 尺寸 | 用途 |
|---|---:|---|
| 重要数字 | `--fs-xl` / `--fs-2xl` | 今日工资、汇总金额、关键统计 |
| 页面标题 | `--fs-xl` | 首页、列表页、详情页标题 |
| 卡片标题 | `--fs-md` | 模块标题、工序名、订单名 |
| 正文 | `--fs-base` | 主要信息、表单内容 |
| 次级说明 | `--fs-sm` | 操作说明、状态解释 |
| 最小文字 | `--fs-xs` 或 `--fs-2xs` | 仅用于时间、单位、辅助说明，不得低对比 |

## 6. 页面结构

### 员工端首页

首页顺序固定为：

1. 员工身份与关键收益信息
2. 考勤打卡
3. 今日报工
4. 快捷操作
5. 退出登录

快捷操作应包含：

- 扫码打卡
- 报工记录
- 我的工资
- 修改密码
- 排行榜（仅开关打开时显示）

修改密码应放在快捷操作网格中，不再藏到深层页面；弹窗继续复用现有修改密码表单。

### 老板端

老板端信息密度可以更高，但要保持分组明确：

- 顶部先给今日/本月关键指标。
- 列表卡片保留清晰主标题和副信息。
- 危险操作必须在视觉上和普通操作分开。
- 订单详情、报工管理、员工管理这类高频页，不要使用过淡的小字表达关键业务字段。

### 质检端

质检端强调“待判断”：

- 待检记录应突出员工、订单、工序、提交数量。
- 合格率、不合格数、员工实得用强对比文字。
- 合格/不合格按钮必须是明确的语义色，不依赖小字解释。

## 7. 组件规范

### 卡片

- 信息卡使用白底、细边框、弱阴影。
- 卡片内标题用 `--text-primary`。
- 卡片内副信息用 `--text-secondary`，不要用低对比硬编码灰。
- 卡片圆角保持 22rpx-30rpx，避免同页圆角风格混乱。

### 快捷操作

- 使用 `.quick-grid` + `.quick-item`。
- 图标容器用 `.quick-item-icon` 的语义色。
- 主标签必须清楚表达动作，例如“修改密码”。
- 描述文字可以短，但必须可读。

### 表单

- label 使用 `--text-secondary`。
- 输入内容使用 `--text-primary`。
- placeholder 使用 `--text-placeholder`。
- 错误提示使用红色语义 token。

### 弹窗

- 弹窗必须高于遮罩层。
- 表单弹窗使用居中 modal sheet。
- 危险确认必须明确说明影响范围。

### 按钮

- 主按钮：蓝底白字。
- 次按钮：浅灰底深色文字。
- 危险按钮：红色文字或红色底。
- 禁用态可以降低透明度，但文字仍需可辨认。

## 8. 交互规则

- 所有可点击卡片应有 `hover-class="tap-card-hover"`。
- 所有按钮应有 `hover-class="tap-button-hover"` 或同类反馈。
- 关键操作要有 loading 态。
- 表单提交前先做前端校验，失败 toast 文案必须具体。
- 长列表和大订单页保持分页、分批渲染，避免一次性堆满屏。

## 9. 反模式

禁止：

- 白底上使用过浅灰色小字。
- 为了“高级感”把关键业务信息做淡。
- 把主操作藏在页面底部或二级页面里。
- 在业务工具里做营销式大 hero。
- 卡片套卡片、过度阴影、厚色条、无意义渐变。
- 使用没有业务含义的装饰文案。
- 新增页面时绕开 `app.wxss` token 自己写一套颜色。

## 10. 后续落地清单

短期：

- 继续清理页面内硬编码文字颜色，统一走 token。
- 对员工端、老板端、质检端各截一张主流程截图做人工检查。
- 把低对比灰字测试保留在 `tests/ui-apple-style.test.js`。

中期：

- 为常用业务组件补充小程序级组件规范：订单卡、工序卡、员工卡、工资卡、报工记录行。
- 把高风险页面的空状态、加载态、失败态统一。
- 建立“新增页面自检表”：字号、颜色、触控区、loading、空状态、错误提示。

长期：

- 可参考 Open Design 的 `DESIGN.md` 结构，把本文件拆成更正式的设计系统文档。
- 可增加截图验收：员工首页、报工页、订单详情、工资详情、质检页。
- 可把颜色对比度测试扩展到更多硬编码颜色，而不是只拦截已知浅灰。

## 11. 参考

- Open Design GitHub: https://github.com/nexu-io/open-design
- Open Design 本地下载位置：`/private/tmp/open-design`
- 当前小程序视觉 token：`miniprogram/app.wxss`
- 当前 UI 回归测试：`tests/ui-apple-style.test.js`
